"use client";
import NolvaLayout from "@/components/layout/nolva-layout";

export default function NolvaGroupLayout({ children }: { children: React.ReactNode }) {
  return <NolvaLayout>{children}</NolvaLayout>;
}
